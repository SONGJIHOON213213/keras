#train dat -> training 중간중간 평가 하는 역활 > validation data 즉 확인하고 검증하기 위해
#validation data를 사용 
이렇게 훈련시키고 한 번도 보지 못한 데이터에 대해서 평가를 해야하는데 이때 사용하는것이 test data
#test data 는 단1번만 사용 되어지며 최종적 모델 평가 지표로 사용 그렇기 때문에 
#train,validation,test 데이터를 나눈다.

from tensorflow.python.keras.models import Sequential
from tensorflow.python.keras.layers import Dense
from sklearn.model_selection import train_test_split
import numpy as np

#1. 데이터
x = np.array(range(1,17)) #X의쉐이프10개 1~16
y = np.array(range(1,17))

#실습 :: 잘라봐!!!
# train_test_split
# 10:3:3

x_train, x_test, y_train, y_test = train_test_split(
x, y, shuffle=True, train_size=0.7, random_state=777
)
X의쉐이프 10개 3 트레인 3 테스트  
